---
phase: 03-test-coverage
plan: "03"
type: execute
---

<objective>
Add integration tests for critical paths spanning multiple packages (core → cli → connect).

Purpose: Integration tests verify that packages work correctly together, catching issues unit tests miss.
Output: Integration test suite covering end-to-end flows like sync, connect, and export.
</objective>

<execution_context>
./.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/TESTING.md
@.planning/phases/03-test-coverage/03-02-SUMMARY.md

**Critical paths to test:**
1. **Sync flow**: CLI command → core sync → storage (with temp files)
2. **Connection flow**: CLI connect → core connection mode selector → relay health check
3. **Export flow**: CLI export → core exporters → file output
4. **Webhook flow**: Connect route → core webhook handler → storage

**Test strategy:**
- Use real implementations where safe (pure functions, utilities)
- Mock external services (Plaid API, Gmail API, Relay server)
- Use temp directories for file system operations
- Follow TESTING.md integration test patterns
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create integration test infrastructure</name>
  <files>packages/core/src/__tests__/integration/setup.ts</files>
  <action>Create shared integration test utilities: temp directory management, mock server helpers, test fixtures for Plaid/Gmail responses. Create IntegrationTestHelpers class with beforeAll/afterAll setup for temp directories. Follow TESTING.md temp directory pattern. Place in core package since it's the foundation.</action>
  <verify>Helper module compiles, exports usable IntegrationTestHelpers class</verify>
  <done>Integration test infrastructure ready for use</done>
</task>

<task type="auto">
  <name>Task 2: Add sync flow integration test</name>
  <files>packages/core/src/__tests__/integration/sync-flow.test.ts</files>
  <action>Create integration test for sync flow: mock Plaid API responses, use temp storage directory, verify transactions stored correctly with deduplication. Test the complete path from Billclaw.syncPlaid() through TransactionStorage. Mock fetch for Plaid API, use real TransactionStorage with temp dir. Verify checkpoint cursor persistence.</action>
  <verify>pnpm test passes, test covers happy path and error handling</verify>
  <done>Sync flow integration test passes</done>
</task>

<task type="auto">
  <name>Task 3: Add export flow integration test</name>
  <files>packages/core/src/__tests__/integration/export-flow.test.ts</files>
  <action>Create integration test for export flow: seed test transactions in temp storage, export to Beancount/Ledger format, verify output structure and content. Use real TransactionStorage and real exporters. Verify date formatting, account naming, and transaction categorization in output.</action>
  <verify>pnpm test passes, export output matches expected format</verify>
  <done>Export flow integration test passes</done>
</task>

<task type="auto">
  <name>Task 4: Add connection mode integration test</name>
  <files>packages/core/src/__tests__/integration/connection-mode.test.ts</files>
  <action>Create integration test for connection mode selection: test Direct/Relay/Polling mode detection with mock health checks. Mock fetch for health check endpoints. Verify mode selection logic with various config scenarios. Test fallback behavior when modes unavailable.</action>
  <verify>pnpm test passes, all mode selection paths tested</verify>
  <done>Connection mode integration test passes</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm build` succeeds without TypeScript errors
- [ ] `pnpm test` passes all tests including integration tests
- [ ] Integration tests use temp directories (cleaned up)
- [ ] External services properly mocked
</verification>

<success_criteria>

- Integration test infrastructure created
- Sync flow integration test passes
- Export flow integration test passes
- Connection mode integration test passes
- All tests pass with proper cleanup
</success_criteria>

<output>
After completion, create `.planning/phases/03-test-coverage/03-03-SUMMARY.md`:

# Phase 3 Plan 3: Integration Tests Summary

**[Substantive one-liner]**

## Accomplishments

- Created integration test infrastructure with temp directory helpers
- Added sync flow integration test
- Added export flow integration test
- Added connection mode integration test
- Phase 3 (Test Coverage) complete

## Files Created/Modified

- `packages/core/src/__tests__/integration/setup.ts`
- `packages/core/src/__tests__/integration/sync-flow.test.ts`
- `packages/core/src/__tests__/integration/export-flow.test.ts`
- `packages/core/src/__tests__/integration/connection-mode.test.ts`

## Decisions Made

[Decisions or "None - followed plan"]

## Issues Encountered

[Any issues or "None"]

## Next Phase Readiness

- Test coverage significantly increased
- Ready for Phase 4: Relay Reconnection
</output>
