---
phase: 03-test-coverage
plan: "01"
type: execute
---

<objective>
Add comprehensive tests for CLI commands (setup, sync, connect, status, config, export) to increase coverage from 8.7% to ~80%.

Purpose: CLI commands are the primary user interface. Tests ensure reliability during refactoring and provide documentation of expected behavior.
Output: Test files for all CLI commands with mocked core dependencies.
</objective>

<execution_context>
./.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/TESTING.md
@.planning/phases/02-code-deduplication/02-01-SUMMARY.md
@.planning/phases/02-code-deduplication/02-02-SUMMARY.md

**Current State:**
- CLI has 23 source files, only 2 test files (logger.test.ts, format.test.ts)
- Coverage: 8.7% (target: 80%)
- Commands to test: setup, sync, connect (plaid, gmail, status), config, status, export
- Pattern: Commands depend on @firela/billclaw-core, mock the core for unit testing

**Files needing tests:**
- `packages/cli/src/commands/setup.ts` - Interactive setup wizard
- `packages/cli/src/commands/sync.ts` - Sync command
- `packages/cli/src/commands/connect/plaid.ts` - Plaid OAuth connection
- `packages/cli/src/commands/connect/gmail.ts` - Gmail OAuth connection
- `packages/cli/src/commands/connect/status.ts` - Connection status
- `packages/cli/src/commands/status.ts` - Account status
- `packages/cli/src/commands/config.ts` - Config management
- `packages/cli/src/commands/export.ts` - Export commands
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test utilities and mock factories for CLI tests</name>
  <files>packages/cli/src/__tests__/test-utils.ts, packages/cli/src/__tests__/mocks/runtime.ts</files>
  <action>Create shared test utilities:
1. `packages/cli/src/__tests__/test-utils.ts` with:
   - `createMockRuntime()` - Returns mock RuntimeContext with logger, config
   - `createMockConfig(overrides?)` - Factory for BillclawConfig
   - `createMockAccount(overrides?)` - Factory for AccountConfig
2. `packages/cli/src/__tests__/mocks/runtime.ts` with MockLogger, MockConfigProvider classes
Pattern follows TESTING.md guidance for factory functions and mock classes.</action>
  <verify>Test files compile without errors, mocks return expected types</verify>
  <done>Shared test utilities available for all CLI command tests</done>
</task>

<task type="auto">
  <name>Task 2: Add tests for setup command</name>
  <files>packages/cli/src/commands/setup.test.ts</files>
  <action>Create test file for setup.ts covering:
- Successful setup flow with valid inputs
- Handling of existing configuration
- Error handling for file system failures
- Interactive prompts (mock inquirer/prompts)
Mock: fs-extra, @firela/billclaw-core getConfig, saveConfig
Follow pattern from TESTING.md: describe/it blocks, beforeEach for setup, afterEach for cleanup.</action>
  <verify>pnpm test -- packages/cli/src/commands/setup.test.ts passes</verify>
  <done>Setup command has >80% test coverage</done>
</task>

<task type="auto">
  <name>Task 3: Add tests for sync command</name>
  <files>packages/cli/src/commands/sync.test.ts</files>
  <action>Create test file for sync.ts covering:
- Sync with valid account ID
- Sync all accounts when no ID specified
- Error handling for invalid account
- Progress display (mock Spinner)
Mock: @firela/billclaw-core Billclaw class, syncPlaid/syncGmail methods
Test both Plaid and Gmail sync paths.</action>
  <verify>pnpm test -- packages/cli/src/commands/sync.test.ts passes</verify>
  <done>Sync command has >80% test coverage</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm build` succeeds without TypeScript errors
- [ ] `pnpm test` passes all tests including new ones
- [ ] New test files follow TESTING.md patterns
- [ ] Mocks properly isolate CLI from core dependencies
</verification>

<success_criteria>

- Test utilities created and reusable
- setup.test.ts created with core scenarios
- sync.test.ts created with core scenarios
- All tests pass
- CLI test coverage increased from 8.7%
</success_criteria>

<output>
After completion, create `.planning/phases/03-test-coverage/03-01-SUMMARY.md`:

# Phase 3 Plan 1: CLI Command Tests (Part 1) Summary

**[Substantive one-liner]**

## Accomplishments

- Created shared test utilities and mock factories
- Added tests for setup command
- Added tests for sync command

## Files Created/Modified

- `packages/cli/src/__tests__/test-utils.ts` - Shared test utilities
- `packages/cli/src/__tests__/mocks/runtime.ts` - Mock classes
- `packages/cli/src/commands/setup.test.ts` - Setup command tests
- `packages/cli/src/commands/sync.test.ts` - Sync command tests

## Decisions Made

[Decisions or "None - followed plan"]

## Issues Encountered

[Any issues or "None"]

## Next Step

Ready for 03-02-PLAN.md (Connect and Status command tests)
</output>
