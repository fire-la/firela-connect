---
phase: 03-test-coverage
plan: "02"
type: execute
---

<objective>
Add tests for remaining CLI commands (connect, status, config, export) and connect package route handlers.

Purpose: Complete CLI test coverage and add tests for connect package Express routes.
Output: Test files for connect, status, config, export commands and route handlers.
</objective>

<execution_context>
./.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/TESTING.md
@.planning/phases/03-test-coverage/03-01-SUMMARY.md

**Current CLI commands needing tests:**
- `connect.ts` - Connection mode commands
- `status.ts` - Account status display
- `config.ts` - Configuration management
- `export.ts` - Beancount/Ledger export

**Connect package routes needing tests:**
- `routes/plaid.ts` - Plaid OAuth endpoints
- `routes/gmail.ts` - Gmail OAuth endpoints
- `routes/webhooks.ts` - Webhook receivers
- `routes/receiver-status.ts` - Status endpoints
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add tests for connect command</name>
  <files>packages/cli/src/commands/connect.test.ts</files>
  <action>Create test file for connect command group. Test connection mode detection using mocked selectConnectionMode. Test error handling for invalid modes. Mock RuntimeContext with test config. Follow TESTING.md patterns with describe/it blocks and vi.mock for core dependencies.</action>
  <verify>pnpm test packages/cli passes, test file follows project conventions</verify>
  <done>connect.test.ts created with core scenarios passing</done>
</task>

<task type="auto">
  <name>Task 2: Add tests for status command</name>
  <files>packages/cli/src/commands/status.test.ts</files>
  <action>Create test file for status command. Test account listing, status display formatting, error handling when no accounts configured. Mock Billclaw class and its getAccounts method. Test output formatting matches expected patterns.</action>
  <verify>pnpm test packages/cli passes, status command scenarios covered</verify>
  <done>status.test.ts created with core scenarios passing</done>
</task>

<task type="auto">
  <name>Task 3: Add tests for config command</name>
  <files>packages/cli/src/commands/config.test.ts</files>
  <action>Create test file for config command. Test config get/set/list operations. Mock config provider with test fixtures. Test validation of config values using Zod schemas. Test error handling for invalid config paths.</action>
  <verify>pnpm test packages/cli passes, config operations tested</verify>
  <done>config.test.ts created with core scenarios passing</done>
</task>

<task type="auto">
  <name>Task 4: Add tests for connect package routes</name>
  <files>packages/connect/src/routes/plaid.test.ts, packages/connect/src/routes/gmail.test.ts, packages/connect/src/routes/webhooks.test.ts</files>
  <action>Create test files for Express route handlers. Use supertest or similar for HTTP endpoint testing. Mock external dependencies (Plaid SDK, fetch). Test OAuth flow endpoints (link token creation, token exchange). Test webhook signature verification and event handling. Follow TESTING.md patterns.</action>
  <verify>pnpm test packages/connect passes, route handlers covered</verify>
  <done>Route test files created with core scenarios passing</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm build` succeeds without TypeScript errors
- [ ] `pnpm test` passes all tests including new ones
- [ ] New test files follow TESTING.md patterns
- [ ] HTTP route tests use proper mocking
</verification>

<success_criteria>

- connect.test.ts created
- status.test.ts created
- config.test.ts created
- Route tests for plaid, gmail, webhooks created
- All tests pass
- Coverage increased for both cli and connect packages
</success_criteria>

<output>
After completion, create `.planning/phases/03-test-coverage/03-02-SUMMARY.md`:

# Phase 3 Plan 2: CLI Commands (Part 2) and Connect Routes Summary

**[Substantive one-liner]**

## Accomplishments

- Added tests for connect, status, config commands
- Added tests for connect package route handlers
- All core CLI commands now have test coverage

## Files Created/Modified

- `packages/cli/src/commands/connect.test.ts`
- `packages/cli/src/commands/status.test.ts`
- `packages/cli/src/commands/config.test.ts`
- `packages/connect/src/routes/plaid.test.ts`
- `packages/connect/src/routes/gmail.test.ts`
- `packages/connect/src/routes/webhooks.test.ts`

## Decisions Made

[Decisions or "None - followed plan"]

## Issues Encountered

[Any issues or "None"]

## Next Step

Ready for 03-03-PLAN.md (Integration tests)
</output>
