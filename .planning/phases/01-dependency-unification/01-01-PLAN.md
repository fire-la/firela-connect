---
phase: 01-dependency-unification
plan: 01
type: execute
---

<objective>
Update TypeScript dependency in connect package from 5.3.3 to 5.8.0 to unify versions across all packages.

Purpose: Eliminate version mismatch that can cause type incompatibility and build issues across the monorepo.
Output: connect package using TypeScript 5.8.0, same as core/cli/openclaw packages.
</objective>

<execution_context>
./.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/STACK.md
@.planning/codebase/CONCERNS.md

**Current State:**
- core package: TypeScript ^5.8.0
- cli package: TypeScript ^5.8.0
- openclaw package: TypeScript ^5.8.0
- connect package: TypeScript ^5.3.3 (OUTDATED)

**Target State:**
All packages using TypeScript ^5.8.0 for consistency.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update TypeScript version in connect package.json</name>
  <files>packages/connect/package.json</files>
  <action>Update devDependencies.typescript from "^5.3.3" to "^5.8.0" to match other packages. Also update related devDependencies to match: @types/node from "^20.10.0" to "^25.2.0", oxlint from "^0.0.11" to "^0.15.0". Do NOT modify dependencies section - only devDependencies.</action>
  <verify>grep -E '"typescript":|"@types/node":|"oxlint":' packages/connect/package.json shows updated versions</verify>
  <done>TypeScript version matches ^5.8.0, @types/node matches ^25.2.0, oxlint matches ^0.15.0</done>
</task>

<task type="auto">
  <name>Task 2: Install updated dependencies and build</name>
  <files>packages/connect/package.json, pnpm-lock.yaml</files>
  <action>Run `pnpm install` to update lockfile with new versions. Then run `pnpm build` to compile all packages and verify TypeScript compatibility. The connect package extends tsconfig.base.json so should work with same settings as other packages.</action>
  <verify>pnpm build exits with code 0, no TypeScript compilation errors</verify>
  <done>All packages build successfully with unified TypeScript 5.8.0</done>
</task>

<task type="auto">
  <name>Task 3: Run tests to verify no regressions</name>
  <files>packages/connect/src/server.test.ts</files>
  <action>Run `pnpm test` from root to execute all tests, ensuring TypeScript update did not break any functionality. Focus on connect package tests but run full suite to catch cross-package issues.</action>
  <verify>pnpm test exits with code 0, all tests pass</verify>
  <done>All tests pass with updated TypeScript version</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm build` succeeds without TypeScript errors
- [ ] `pnpm test` passes all tests
- [ ] connect package.json shows TypeScript ^5.8.0
- [ ] No type errors in IDE or compilation
</verification>

<success_criteria>

- TypeScript version unified to ^5.8.0 across all packages
- All packages build successfully
- All tests pass
- No runtime or type errors introduced
</success_criteria>

<output>
After completion, create `.planning/phases/01-dependency-unification/01-01-SUMMARY.md`:

# Phase 1 Plan 1: TypeScript Update Summary

**Unified connect package TypeScript to 5.8.0**

## Accomplishments

- Updated TypeScript from 5.3.3 to 5.8.0
- Updated related devDependencies to match other packages
- Verified build and test compatibility

## Files Created/Modified

- `packages/connect/package.json` - Updated devDependencies versions
- `pnpm-lock.yaml` - Updated lockfile (auto-generated)

## Decisions Made

Updated all devDependencies to match other packages for consistency.

## Issues Encountered

[Any issues or "None"]

## Next Step

Ready for 01-02-PLAN.md (Vitest update)
</output>
