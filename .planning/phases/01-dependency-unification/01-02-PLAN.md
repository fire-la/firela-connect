---
phase: 01-dependency-unification
plan: 02
type: execute
---

<objective>
Update Vitest dependency in connect package from 1.1.0 to 3.0.0 to unify versions across all packages.

Purpose: Eliminate version mismatch that can cause test configuration incompatibility and inconsistent test behavior across the monorepo.
Output: connect package using Vitest 3.0.0, same as core/cli/openclaw packages.
</objective>

<execution_context>
./.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/STACK.md
@.planning/codebase/TESTING.md
@.planning/codebase/CONCERNS.md

**Current State:**
- core package: Vitest ^3.0.0
- cli package: Vitest ^3.0.0
- openclaw package: Vitest ^3.0.0
- connect package: Vitest ^1.1.0 (OUTDATED)

**Vitest Config Differences:**
- connect: Simple config (environment: node, watch: false)
- core: Extended config (globals: true, include pattern, alias, optimizeDeps)

**Target State:**
All packages using Vitest ^3.0.0 with consistent configuration.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Vitest version in connect package.json</name>
  <files>packages/connect/package.json</files>
  <action>Update devDependencies.vitest from "^1.1.0" to "^3.0.0" to match other packages. Also add @vitest/coverage-v8 "^4.0.18" for coverage support (matching other packages). Do NOT modify other dependencies.</action>
  <verify>grep -E '"vitest":|"@vitest/coverage-v8":' packages/connect/package.json shows updated versions</verify>
  <done>Vitest version matches ^3.0.0, @vitest/coverage-v8 added</done>
</task>

<task type="auto">
  <name>Task 2: Update connect vitest.config.ts to match other packages</name>
  <files>packages/connect/vitest.config.ts</files>
  <action>Update vitest.config.ts to match the pattern used by core package: add `globals: true`, add `include: ['src/**/*.test.ts']` pattern. Keep existing `environment: 'node'` and `watch: false` settings. Remove watch: false if it's not needed (Vitest 3.0 handles this differently). Follow the core package config structure.</action>
  <verify>cat packages/connect/vitest.config.ts shows updated config with globals and include pattern</verify>
  <done>Vitest config updated to Vitest 3.0 patterns matching other packages</done>
</task>

<task type="auto">
  <name>Task 3: Install dependencies and run tests</name>
  <files>packages/connect/package.json, pnpm-lock.yaml</files>
  <action>Run `pnpm install` to update lockfile. Then run `pnpm test` from root to verify all tests pass with updated Vitest. The connect package has only 1 test file (server.test.ts) so should be quick.</action>
  <verify>pnpm test exits with code 0, connect package tests pass</verify>
  <done>All tests pass with unified Vitest 3.0.0</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm test` passes all tests including connect package
- [ ] connect package.json shows Vitest ^3.0.0
- [ ] vitest.config.ts has consistent config with other packages
- [ ] No test failures or deprecation warnings
</verification>

<success_criteria>

- Vitest version unified to ^3.0.0 across all packages
- All tests pass
- Vitest config consistent with other packages
- No test runtime errors introduced
</success_criteria>

<output>
After completion, create `.planning/phases/01-dependency-unification/01-02-SUMMARY.md`:

# Phase 1 Plan 2: Vitest Update Summary

**Unified connect package Vitest to 3.0.0**

## Accomplishments

- Updated Vitest from 1.1.0 to 3.0.0
- Added @vitest/coverage-v8 for coverage support
- Updated vitest.config.ts to match other packages
- Verified test compatibility

## Files Created/Modified

- `packages/connect/package.json` - Updated Vitest and added coverage package
- `packages/connect/vitest.config.ts` - Updated config for Vitest 3.0
- `pnpm-lock.yaml` - Updated lockfile (auto-generated)

## Decisions Made

Aligned vitest.config.ts with core package pattern for consistency.

## Issues Encountered

[Any issues or "None"]

## Next Step

Phase 1 complete. Ready for Phase 2: Code Deduplication
</output>
