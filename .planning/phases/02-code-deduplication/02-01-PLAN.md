---
phase: 02-code-deduplication
plan: "01"
type: execute
---

<objective>
Centralize formatUserCode utility function in core package to eliminate code duplication.

Purpose: The formatUserCode function is duplicated in both cli and openclaw packages. Centralizing it in core reduces maintenance burden and prevents divergence.
Output: Single formatUserCode function in core package, imported by cli and openclaw.
</objective>

<execution_context>
./.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/CONCERNS.md
@.planning/phases/01-dependency-unification/01-02-SUMMARY.md

**Current State:**
- `packages/cli/src/commands/connect/gmail.ts:437-443` - formatUserCode function
- `packages/openclaw/src/tools/connect-gmail.tool.ts:43-49` - identical formatUserCode function

**Target State:**
- `packages/core/src/utils/format.ts` - centralized formatUserCode function
- cli and openclaw import from `@firela/billclaw-core/utils/format`
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create formatUserCode utility in core package</name>
  <files>packages/core/src/utils/format.ts, packages/core/src/index.ts</files>
  <action>Create new file `packages/core/src/utils/format.ts` with formatUserCode function. The function formats 8-character Google OAuth user codes as XXXX-XXXX format. Export from core index.ts under a `/utils/format` subpath export for tree-shaking. Follow existing core export patterns.</action>
  <verify>grep -r "formatUserCode" packages/core/src/utils/format.ts shows function exists, grep "utils/format" packages/core/src/index.ts shows export</verify>
  <done>formatUserCode utility created and exported from core package</done>
</task>

<task type="auto">
  <name>Task 2: Update cli package to use centralized formatUserCode</name>
  <files>packages/cli/src/commands/connect/gmail.ts</files>
  <action>Remove the local formatUserCode function (lines ~437-443). Add import from `@firela/billclaw-core/utils/format`. Update any references to use the imported function. Keep existing usage at line 259 unchanged.</action>
  <verify>grep -c "function formatUserCode" packages/cli/src/commands/connect/gmail.ts returns 0 (function removed), grep "formatUserCode" packages/cli/src/commands/connect/gmail.ts still shows usage via import</verify>
  <done>cli package uses centralized formatUserCode from core</done>
</task>

<task type="auto">
  <name>Task 3: Update openclaw package to use centralized formatUserCode</name>
  <files>packages/openclaw/src/tools/connect-gmail.tool.ts</files>
  <action>Remove the local formatUserCode function (lines ~43-49). Add import from `@firela/billclaw-core/utils/format`. Update all references (lines 326, 334, 345, 350) to use the imported function.</action>
  <verify>grep -c "function formatUserCode" packages/openclaw/src/tools/connect-gmail.tool.ts returns 0 (function removed), grep "formatUserCode" packages/openclaw/src/tools/connect-gmail.tool.ts still shows usage via import</verify>
  <done>openclaw package uses centralized formatUserCode from core</done>
</task>

<task type="auto">
  <name>Task 4: Build and test to verify no regressions</name>
  <files>packages/core/src/utils/format.ts, packages/cli/src/commands/connect/gmail.ts, packages/openclaw/src/tools/connect-gmail.tool.ts</files>
  <action>Run `pnpm build` to verify TypeScript compilation. Run `pnpm test` to verify all tests pass. The formatUserCode change is a pure refactor with no behavior change, so all existing tests should pass.</action>
  <verify>pnpm build exits 0, pnpm test exits 0</verify>
  <done>All packages build and test successfully with centralized formatUserCode</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm build` succeeds without TypeScript errors
- [ ] `pnpm test` passes all tests
- [ ] formatUserCode exists only in core package
- [ ] cli and openclaw import formatUserCode from core
</verification>

<success_criteria>

- formatUserCode centralized in core package
- No duplicate formatUserCode functions in cli or openclaw
- All packages build successfully
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-code-deduplication/02-01-SUMMARY.md`:

# Phase 2 Plan 1: formatUserCode Centralization Summary

**Centralized formatUserCode utility in core package**

## Accomplishments

- Created formatUserCode utility in core/utils/format.ts
- Removed duplicate function from cli and openclaw packages
- Updated imports to use centralized function

## Files Created/Modified

- `packages/core/src/utils/format.ts` - New utility file
- `packages/core/src/index.ts` - Added export
- `packages/cli/src/commands/connect/gmail.ts` - Removed local function, added import
- `packages/openclaw/src/tools/connect-gmail.tool.ts` - Removed local function, added import

## Decisions Made

[Decisions or "None - followed plan"]

## Issues Encountered

[Any issues or "None"]

## Next Step

Ready for 02-02-PLAN.md (RELAY_URL centralization)
</output>
