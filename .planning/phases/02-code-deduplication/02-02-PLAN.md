---
phase: 02-code-deduplication
plan: "02"
type: execute
---

<objective>
Centralize RELAY_URL constant in core package configuration to eliminate hardcoded URLs across packages.

Purpose: The relay.firela.io URL is hardcoded in 20+ locations across cli, openclaw, and core packages. Centralizing it improves deployment flexibility and reduces maintenance burden.
Output: Single RELAY_URL source in core config, imported/used by all packages.
</objective>

<execution_context>
./.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/CONCERNS.md
@.planning/phases/02-code-deduplication/02-01-SUMMARY.md

**Current State:**
The relay URL is hardcoded in multiple locations:
- `packages/cli/src/commands/connect/gmail.ts:23` - RELAY_URL constant
- `packages/cli/src/commands/connect/plaid.ts:88,222` - inline string
- `packages/openclaw/src/tools/connect-gmail.tool.ts:38` - RELAY_URL constant
- `packages/openclaw/src/tools/connect-plaid.tool.ts:149,294` - inline string
- `packages/openclaw/src/services/oauth-completion.ts:161,260` - inline string
- `packages/core/src/webhook/config.ts` - already has RELAY_URLS config with defaults
- `packages/core/src/webhook/helpers.ts` - already uses centralized constants
- `packages/core/src/relay/oauth.ts` - already uses centralized constants

**Target State:**
- Export RELAY_URL constant from `packages/core/src/connection/constants.ts`
- All packages import from core instead of hardcoding
- Existing webhook config remains authoritative for webhook-specific URLs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create RELAY_URL constant in core package</name>
  <files>packages/core/src/connection/constants.ts, packages/core/src/connection/index.ts</files>
  <action>Create new file `packages/core/src/connection/constants.ts` with RELAY_URL constant set to "https://relay.firela.io". Export from connection/index.ts. This provides a single source of truth for the relay URL that can be used across packages. Note: The existing webhook config in webhook/config.ts has more detailed URL variants (wsUrl, apiUrl, oauthUrl) - keep those for webhook-specific use cases. This new constant is for the base URL used by OAuth and other non-webhook flows.</action>
  <verify>grep "RELAY_URL" packages/core/src/connection/constants.ts shows constant, grep "RELAY_URL" packages/core/src/connection/index.ts shows export</verify>
  <done>RELAY_URL constant created and exported from core connection module</done>
</task>

<task type="auto">
  <name>Task 2: Update cli package to use centralized RELAY_URL</name>
  <files>packages/cli/src/commands/connect/gmail.ts, packages/cli/src/commands/connect/plaid.ts</files>
  <action>In gmail.ts: Remove local RELAY_URL constant (line ~23), import from `@firela/billclaw-core/connection`. In plaid.ts: Replace inline "https://relay.firela.io" strings (lines ~88, 222) with imported RELAY_URL constant.</action>
  <verify>grep -c "const RELAY_URL" packages/cli/src/commands/connect/gmail.ts returns 0, grep "relay.firela.io" packages/cli/src/commands/connect/plaid.ts returns 0 or only shows import</verify>
  <done>cli package uses centralized RELAY_URL from core</done>
</task>

<task type="auto">
  <name>Task 3: Update openclaw package to use centralized RELAY_URL</name>
  <files>packages/openclaw/src/tools/connect-gmail.tool.ts, packages/openclaw/src/tools/connect-plaid.tool.ts, packages/openclaw/src/services/oauth-completion.ts</files>
  <action>In connect-gmail.tool.ts: Remove local RELAY_URL constant (line ~38), import from `@firela/billclaw-core/connection`. In connect-plaid.tool.ts: Replace inline strings (lines ~149, 294). In oauth-completion.ts: Replace inline strings (lines ~161, 260).</action>
  <verify>grep -c "const RELAY_URL" packages/openclaw/src/tools/connect-gmail.tool.ts returns 0, grep -c "relay.firela.io" packages/openclaw/src/tools/connect-plaid.tool.ts returns 0, grep -c "relay.firela.io" packages/openclaw/src/services/oauth-completion.ts returns 0</verify>
  <done>openclaw package uses centralized RELAY_URL from core</done>
</task>

<task type="auto">
  <name>Task 4: Build and test to verify no regressions</name>
  <files>Multiple files across packages</files>
  <action>Run `pnpm build` to verify TypeScript compilation. Run `pnpm test` to verify all tests pass. This is a pure refactor replacing hardcoded strings with a constant - no behavior change.</action>
  <verify>pnpm build exits 0, pnpm test exits 0</verify>
  <done>All packages build and test successfully with centralized RELAY_URL</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm build` succeeds without TypeScript errors
- [ ] `pnpm test` passes all tests
- [ ] RELAY_URL constant exists in core/connection/constants.ts
- [ ] No hardcoded "relay.firela.io" strings in cli or openclaw packages (except comments/docs)
- [ ] All packages import RELAY_URL from core
</verification>

<success_criteria>

- RELAY_URL centralized in core package
- No hardcoded relay URL strings in cli or openclaw
- All packages build successfully
- All tests pass
- Phase 2 (Code Deduplication) complete
</success_criteria>

<output>
After completion, create `.planning/phases/02-code-deduplication/02-02-SUMMARY.md`:

# Phase 2 Plan 2: RELAY_URL Centralization Summary

**Centralized RELAY_URL constant in core package**

## Accomplishments

- Created RELAY_URL constant in core/connection/constants.ts
- Removed hardcoded URLs from cli and openclaw packages
- Updated all packages to import from core

## Files Created/Modified

- `packages/core/src/connection/constants.ts` - New constants file
- `packages/core/src/connection/index.ts` - Added export
- `packages/cli/src/commands/connect/gmail.ts` - Removed local constant, added import
- `packages/cli/src/commands/connect/plaid.ts` - Replaced inline strings
- `packages/openclaw/src/tools/connect-gmail.tool.ts` - Removed local constant, added import
- `packages/openclaw/src/tools/connect-plaid.tool.ts` - Replaced inline strings
- `packages/openclaw/src/services/oauth-completion.ts` - Replaced inline strings

## Decisions Made

[Decisions or "None - followed plan"]

## Issues Encountered

[Any issues or "None"]

## Next Step

Phase 2 complete. Ready for Phase 3: Test Coverage
</output>
